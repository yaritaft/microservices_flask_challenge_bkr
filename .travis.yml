
os:
    - linux
dist: trusty

distro: alpine
# sudo: required


script:
    - docker-compose -f docker-compose.yml -f docker-compose-test.yml build && docker-compose -f docker-compose.yml -f docker-compose-test.yml up -d --force-recreate

